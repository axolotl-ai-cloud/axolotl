# Keep .git so in-container `git pull` keeps working; only drop non-package noise.

# Embedded clones / local build trees that aren't part of the package
mamba/
flash-attention/
sonic-moe/
cuda-kernels/
benchmark_kernels/

# Python / tooling caches
**/__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local run artifacts
outputs/
last_run_prepared/

# Editor / OS
.DS_Store
.vscode/
.idea/
