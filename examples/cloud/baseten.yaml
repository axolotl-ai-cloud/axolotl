provider: baseten
project_name:

secrets:
  - HF_TOKEN
  - WANDB_API_KEY

gpu: h100
gpu_count: 8
node_count: 1

# Use a prebuilt image containing your fork and plugins:
# image: registry.example.com/team/axolotl:my-fork

# Or build a local source context (paths are relative to this YAML):
# image_build:
#   context: ../my-training-source
#   dockerfile: Dockerfile
#   tag: registry.example.com/team/axolotl:my-fork
